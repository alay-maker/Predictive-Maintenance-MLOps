name: MLOps CI/CD Pipeline

on:
  push:
    # 💡 Añadimos ambas ramas para que funcione ahora y en la entrega final
    branches: [ "ajustes_azure", "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Descargar código
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: |
          pip install -r requirements.txt
          pip install pytest pytest-cov

      - name: Ejecutar Tests con Cobertura >= 70%
        env:
          PYTHONPATH: .
        run: |
          pytest tests/ --cov=src --cov-fail-under=70 

  deploy:
    needs: test
    runs-on: ubuntu-latest
    # 💡 CORRECCIÓN: Permite desplegar si estamos en 'ajustes_azure' o en 'main'
    if: (github.ref == 'refs/heads/ajustes_azure' || github.ref == 'refs/heads/main') && github.event_name == 'push'
    
    steps:
      - name: Descargar código
        uses: actions/checkout@v4

      - name: Copiar archivos al servidor
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.AZURE_VM_IP }}
          username: adminuser
          key: ${{ secrets.AZURE_VM_SSH_KEY }}
          source: "."
          target: "/home/adminuser/predictive-maintenance"

      - name: Desplegar Microservicios en Azure
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.AZURE_VM_IP }}
          username: adminuser
          key: ${{ secrets.AZURE_VM_SSH_KEY }}
          script: |
            cd /home/adminuser/predictive-maintenance
            sudo docker-compose down
            sudo docker-compose up --build -d