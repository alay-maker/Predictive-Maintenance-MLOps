name: MLOps CI/CD Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  # FASE 1: INTEGRACIÓN CONTINUA (CI) - Tests y Cobertura
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Descargar código
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: |
          pip install -r requirements.txt
          pip install pytest pytest-cov

      - name: Ejecutar Tests con Cobertura >= 70%
        # Esto fallará automáticamente si la cobertura baja del 70%
        run: |
          pytest tests/ --cov=src --cov-fail-under=70 

  # FASE 2: DESPLIEGUE CONTINUO (CD) - Enviar al servidor Terraform
  deploy:
    needs: test # Solo se ejecuta si los tests de arriba aprueban
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    steps:
      - name: Descargar código
        uses: actions/checkout@v4

      # Copiamos los archivos al servidor de Azure de forma segura
      - name: Copiar archivos al servidor
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.AZURE_VM_IP }}
          username: adminuser
          key: ${{ secrets.AZURE_VM_SSH_KEY }}
          source: "."
          target: "/home/adminuser/predictive-maintenance"

      # Entramos al servidor y levantamos la arquitectura Docker Compose
      - name: Desplegar Microservicios en Azure
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.AZURE_VM_IP }}
          username: adminuser
          key: ${{ secrets.AZURE_VM_SSH_KEY }}
          script: |
            cd /home/adminuser/predictive-maintenance
            # Apagamos contenedores viejos y levantamos los nuevos en segundo plano (-d)
            sudo docker-compose down
            sudo docker-compose up --build -d